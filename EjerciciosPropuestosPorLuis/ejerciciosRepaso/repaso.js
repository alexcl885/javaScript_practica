//creacion de un array inicializando valores
const array = new Array(10);

for (let i=0; i < array.length; i++) {
    array[i] = 0;
    array[i] += i;
}
console.log(array);

//comprobacion
const arr = ["miguelon ",2, " es" , "un", "maquina"].join();
console.log(arr);

    